name: example
description: "A demonstration of VersionWidget."
publish_to: 'none'
version: 0.0.1+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  # version_widget: ^1.0.5
  version_widget:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
